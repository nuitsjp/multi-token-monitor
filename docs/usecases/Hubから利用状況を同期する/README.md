# Hubから利用状況を同期する

## 主アクター
利用者（アプリケーションを起動・終了する）

## 目的
設定した各Hubの最新利用状況をローカルのSQLiteに保存し、閲覧時にHubへ接続しなくても最新状態を利用できるようにする。保存するたびに閲覧側へ変更を知らせ、閲覧側が最新状態に追従できるようにする。

## 前提
1件以上のHubについて、ID・表示名・URL・認証トークンを記載した接続設定ファイルがGit管理外に用意され、`.env` の `HUB_CONFIG_PATH` がそのファイルを指している。

## 共通の受け入れ条件
- URLと認証トークンをDB、ログ、閲覧側へ渡すデータに含めない。アプリケーションは設定ファイルを更新しない。
- Hubごとに最新状態、受信時刻、受信状態（受信中・再接続中）だけを保持し、受信履歴は保存しない。同じ情報を再受信しても利用量を二重に加算しない。
- あるHubの失敗は、他のHubの受信とWebサーバーに波及しない。

## シナリオ
- [設定したHubの最新状態を受信して保存する](scenarios/設定したHubの最新状態を受信して保存する.md)
- [受信が止まったHubへ再接続する](scenarios/受信が止まったHubへ再接続する.md)

## 未着手のシナリオ
主成功シナリオの完了後に1本ずつ全文を検討する候補。名称と分岐条件だけを置く。

- 接続設定が不正なため起動しない: ファイルがない、JSONが不正、必須項目の欠落、URLの形式不正、Hubが0件、IDの重複
- 保存済みの状態があるまま再起動する: 以前の受信で保存した状態がある
- 表示名やURLを変えて再起動する: 同じIDで表示名やURLを変えた設定で起動した
- 設定から外したHubを残したまま起動する: 以前登録したHubが設定から消えている

## 実現パターン
- [UCP-1. Hubから受信した最新状態をDBへ反映する](../../design/UCP-1.md)
- [UCP-3. DBへの保存確定を画面へ通知し、取得し直させる](../../design/UCP-3.md)
