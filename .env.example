# ローカル実行の設定。バックエンドはloopback（127.0.0.1 または ::1）だけで待ち受ける。
HOST=127.0.0.1
PORT=3000
DB_PATH=./data/app.sqlite
# Hub接続設定ファイル（Git管理外）。config/hubs.example.json を複製して作成する。
HUB_CONFIG_PATH=./data/hubs.local.json
Logging__LogLevel__Default=Information
