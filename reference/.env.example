# ローカル実行。shared server では認証プロキシ設定を docs/project.md に従って変更する。
HOST=127.0.0.1
PORT=3000
DB_PATH=./data/app.sqlite
AUTH_MODE=demo
Logging__LogLevel__Default=Information
# AUTH_MODE=proxy の場合に必須。認証プロキシだけがAPIへ接続できること。
# PUBLIC_ORIGIN=https://app.example.com
# x-authenticated-user はプロキシで必ず削除・再設定する。
